On blacklight, the netcdf4 lib with parallel computing capability is version 4.1.3 compiled with phdf5 1.8.7. So we have to change configure.{wps|wrf} to add phdf5 lib path, otherwise netcdf lib linking fails. 
